
  # TripFlow Travel Planner Prototype

  This is a code bundle for TripFlow Travel Planner Prototype. The original project is available at https://www.figma.com/design/xWZsbp5duQxVnzMDhlWxxY/TripFlow-Travel-Planner-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  